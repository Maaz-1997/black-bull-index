# Black Bull Index — environment variables
# Copy to `.env` for local dev (bun run dev). For Cloudflare Workers, set these as
# Worker secrets (wrangler secret put HELIUS_API_KEY) — never commit real keys.

# Helius — free account at https://helius.dev (Solana wallet data)
HELIUS_API_KEY=

# Groq — free account at https://console.groq.com (LLM roasts, wired in Phase 2)
GROQ_API_KEY=
